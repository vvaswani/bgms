<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Weekly Report - {{ name }}</title>
</head>
<body>
    <h1>Weekly Report for {{ name }}</h1>
    <p>Date: {{ startDate|date('d M Y', timezone) }} to {{ endDate|date('d M Y', timezone) }} ({{ timezone }})</p>

    <h2>Readings</h2>
    <table border="1" cellpadding="6" cellspacing="0">
        <thead>
            <tr>
                <th>Date & Time</th>
                <th>Value</th>
                <th>Fasting</th>
            </tr>
        </thead>
        <tbody>
            {% for reading in readings %}
                <tr>
                    <td>{{ reading.createdAt|date('d M Y H:i T', timezone) }}</td>
                    <td>{{ reading.value }}</td>
                    <td>{{ reading.isFasting ? 'Yes' : 'No' }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <p><strong>Average Value:</strong> {{ average }}</p>

    <h2>AI Analysis</h2>
    <p>{{ analysis|nl2br }}</p>
</body>
</html>
