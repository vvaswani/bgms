<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Daily Report - {{ name }}</title>
</head>
<body>
    <h1>Daily Report for {{ name }}</h1>
    <p>Date: {{ date|date('d M Y', timezone) }} ({{ timezone }})</p>

    <h2>Readings</h2>
    <table border="1" cellpadding="6" cellspacing="0">
        <thead>
            <tr>
                <th>Date & Time</th>
                <th>Value</th>
                <th>Type</th>
            </tr>
        </thead>
        <tbody>
            {% for reading in readings %}
                <tr>
                    <td>{{ reading.createdAt|date('d M Y H:i T', timezone) }}</td>
                    <td>{{ reading.value }}</td>
                    <td>
                        {% if reading.type == constant('App\\Entity\\Reading::TYPE_FASTING') %}
                            Fasting
                        {% elseif reading.type == constant('App\\Entity\\Reading::TYPE_POST_PRANDIAL') %}
                            Post-prandial
                        {% else %}
                            Random
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <p><strong>Average Value:</strong> {{ average }}</p>
</body>
</html>
